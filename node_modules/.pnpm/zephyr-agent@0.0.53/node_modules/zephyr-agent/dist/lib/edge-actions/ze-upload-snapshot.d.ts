import { type Snapshot } from 'zephyr-edge-contract';
import type { ZephyrEngine } from '../../zephyr-engine';
interface ZeUploadSnapshotProps {
    snapshot: Snapshot;
}
export declare function zeUploadSnapshot(zephyr_engine: ZephyrEngine, { snapshot }: ZeUploadSnapshotProps): Promise<string>;
export {};
